mixin video(info)
    //- mixin은 html을 return 해주는 일종의 function을 가지고 있다.
    div 
        h4
            a(href=`/videos/${info.id}`)=info.title
            //- a(href="/videos/" #{info.id}) 는 에러난다. 
            //- pug의 규칙 : attribute에는 #{} 형식 사용 불가능
            //- 백틱(``)을 이용하거나 +info.id 형식으로 이어준다.
        ul 
            li #{info.rating}/5
            li #{info.comments} commetns 
            li Posted #{createdAt}
            li #{info.views} views.